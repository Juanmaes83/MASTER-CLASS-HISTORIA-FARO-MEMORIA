const {test,expect}=require('@playwright/test');
test.setTimeout(120000);
const sceneIds=['hero','coast','keeper','stair','machine','beam','afterlight'];
async function openStory(page){await page.goto('/storytelling.html',{waitUntil:'domcontentloaded',timeout:45000});await page.waitForFunction(()=>window.__FARO_QA__?.ready===true,null,{timeout:60000});await page.waitForSelector('#support-trigger',{state:'attached'});await page.waitForTimeout(250)}
async function jump(page,id,f=.38){await page.evaluate(({id,f})=>{const e=document.getElementById(id);scrollTo(0,e.offsetTop+Math.min(e.offsetHeight*f,520))},{id,f});await page.waitForTimeout(450)}
async function geometry(page){return page.evaluate(ids=>({scrollHeight:document.documentElement.scrollHeight,bodyHeight:document.body.scrollHeight,scrollWidth:document.documentElement.scrollWidth,clientWidth:document.documentElement.clientWidth,scenes:Object.fromEntries(ids.map(id=>{const e=document.getElementById(id),r=e.getBoundingClientRect();return[id,{offsetTop:e.offsetTop,offsetHeight:e.offsetHeight,width:r.width,height:r.height}]})),supportInsidePage:!!document.querySelector('.page #support-trigger,.page #support-deck,.page #answer-event'),supportInsideSection:!!document.querySelector('.sec #support-trigger,.sec #support-deck,.sec #answer-event')}),ids)}
test('Rule 0: support overlay never changes frozen Faro geometry or scroll position',async({page})=>{await page.setViewportSize({width:1440,height:1000});await openStory(page);await jump(page,'coast');const before=await geometry(page),y0=await page.evaluate(()=>scrollY);expect(before.supportInsidePage).toBeFalsy();expect(before.supportInsideSection).toBeFalsy();await expect(page.locator('#support-trigger')).toBeVisible();await expect(page.locator('#support-trigger')).toContainText('WEATHER INSIDE');await page.locator('#support-trigger').click();await expect(page.locator('#support-deck')).toHaveClass(/open/);const open=await geometry(page),yOpen=await page.evaluate(()=>scrollY);expect(open).toEqual(before);expect(yOpen).toBe(y0);await page.mouse.wheel(0,900);await page.waitForTimeout(150);expect(await page.evaluate(()=>scrollY)).toBe(y0);await page.locator('#support-close').click();await page.waitForTimeout(150);expect(await page.evaluate(()=>scrollY)).toBe(y0);const closed=await geometry(page);expect(closed).toEqual(before)});
test('Progressive reveal: Coast / silence / Stair / silence / Beam / Afterlight',async({page})=>{await page.setViewportSize({width:1440,height:1000});await openStory(page);await jump(page,'coast');await expect(page.locator('#support-trigger')).toBeVisible();await expect(page.locator('#support-trigger')).toContainText('WEATHER INSIDE');await jump(page,'keeper');await expect(page.locator('#support-trigger')).toBeHidden();await jump(page,'stair');await expect(page.locator('#support-trigger')).toBeVisible();await expect(page.locator('#support-trigger')).toContainText('ONE STEP');await jump(page,'machine');await expect(page.locator('#support-trigger')).toBeHidden();await jump(page,'beam');await expect(page.locator('#support-trigger')).toBeVisible();await expect(page.locator('#support-trigger')).toContainText('SEND A SIGNAL');await jump(page,'afterlight');await expect(page.locator('#support-trigger')).toBeVisible();await expect(page.locator('#support-trigger')).toContainText('ANOTHER STATION')});
test('Five actions: Fog → One Step → Signal → symbolic light → real stations',async({page})=>{await page.setViewportSize({width:1440,height:1000});await openStory(page);await jump(page,'coast');await page.locator('#support-trigger').click();await expect(page.locator('#support-content')).toContainText('How is the weather inside?');await page.locator('[data-choice="fog"]').click();await expect(page.locator('.deck-result')).toContainText('next visible marker');await page.locator('#support-close').click();await jump(page,'stair');await page.locator('#support-trigger').click();await expect(page.locator('#support-content')).toContainText('Only the next one');await page.locator('[data-choice="tell"]').click();await expect(page.locator('.deck-result')).toContainText('I am having a hard time');await page.locator('#support-close').click();await jump(page,'beam');await page.locator('#support-trigger').click();await expect(page.locator('#support-content')).toContainText('You do not have to tell the whole story');await page.locator('[data-signal="listen"]').click();await expect(page.locator('.signal-template')).toContainText('Could you listen for a while?');await page.locator('#copy-signal').click();await expect(page.locator('#answer-event')).toHaveClass(/show/);await expect(page.locator('#answer-copy')).toContainText('A second light appears');await expect(page.locator('#answer-copy')).toContainText('symbolic');await expect(page.locator('.answer-core')).toBeVisible();await page.locator('#answer-return').click();await jump(page,'afterlight');await page.locator('#support-trigger').click();await expect(page.locator('#support-deck')).toHaveClass(/station-mode/);const stations=page.locator('#support-content');for(const t of ['717 003 717','965 131 122','965 112 125','024','112'])await expect(stations).toContainText(t)});
test('Mobile ES: progressive labels, actions and zero horizontal break',async({page})=>{await page.setViewportSize({width:390,height:844});await openStory(page);const before=await geometry(page);await page.locator('[data-lang="es"]').click();await expect(page.locator('html')).toHaveAttribute('lang','es');await jump(page,'coast');await expect(page.locator('#support-trigger')).toContainText('TIEMPO');await page.locator('#support-trigger').click();await expect(page.locator('#support-content')).toContainText('¿Qué tiempo hace por dentro?');await page.locator('#support-close').click();await jump(page,'stair');await expect(page.locator('#support-trigger')).toContainText('UN PASO');await jump(page,'beam');await expect(page.locator('#support-trigger')).toContainText('ENVIAR UNA SEÑAL');await jump(page,'afterlight');await expect(page.locator('#support-trigger')).toContainText('OTRA ESTACIÓN');await page.locator('#support-trigger').click();await expect(page.locator('#support-content')).toContainText('Teléfono de la Esperanza');await expect(page.locator('#support-content')).toContainText('Proyecto Hombre');const after=await geometry(page);expect(after.scrollHeight).toBe(before.scrollHeight);expect(after.bodyHeight).toBe(before.bodyHeight);expect(after.scrollWidth).toBeLessThanOrEqual(after.clientWidth)});
